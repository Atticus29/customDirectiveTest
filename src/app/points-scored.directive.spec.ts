import { PointsScoredDirective } from './points-scored.directive';

describe('PointsScoredDirective', () => {
  it('should create an instance', () => {
    const directive = new PointsScoredDirective();
    expect(directive).toBeTruthy();
  });
});
